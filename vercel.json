{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/node",
      "config": {
        "distDir": "dist/server",
        "maxDuration": 10
      }
    },
    {
      "use": "@vercel/static",
      "config": {
        "distDir": "dist/client"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/server/server.js"
    }
  ]
}
